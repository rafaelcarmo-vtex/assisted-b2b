/** Standard row height for order canvas tables and field lists (px). */
export const ORDER_ROW_HEIGHT_PX = 48;

/** Tailwind/CSS class bound to --order-row-height in index.html */
export const ORDER_ROW_HEIGHT_CLASS = 'order-row-height';

/** Column label rows only — height/padding, no hover or selection */
export const ORDER_ROW_HEADER_CLASS = ORDER_ROW_HEIGHT_CLASS;
